import styled from 'styled-components'
import ModalWindowWrapper from '@/Components/ModalWindow'

export const FormWindow = styled(ModalWindowWrapper)`
  max-width: 450px;
  width: 30%;
  margin: auto;
`
