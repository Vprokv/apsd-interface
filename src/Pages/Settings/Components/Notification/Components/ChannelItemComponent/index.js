import React from 'react';
import PropTypes from 'prop-types';

const ChannelItemComponent = props => {
  return (
    <div>

    </div>
  );
};

ChannelItemComponent.propTypes = {};

export default ChannelItemComponent;