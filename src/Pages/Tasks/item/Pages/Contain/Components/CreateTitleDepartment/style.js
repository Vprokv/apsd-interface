import styled from 'styled-components'

export const ToggleBtn = styled.div`
  border-bottom: 1px solid var(--form-elements-border-color);
  cursor: pointer;
  &:hover {
    background: var(--notifications);
  }
`
